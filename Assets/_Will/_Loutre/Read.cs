/*
 
float _theta = (2*Mathf.PI / _obj.option.Lentht) * i;
float _xPos = Mathf.Sin(_theta);
float _yPos = Mathf.Cos(_theta);
newButton.transform.localPosition = new Vector3(_xPos, _yPos, 0f)*100f;

 */